---
import Header from '@src/components/Header.astro';
import Footer from '@src/components/Footer.astro';
import { Font } from "astro:assets";

import 'glightbox/dist/css/glightbox.min.css';
import '@src/styles/style.css';
import BackToTop from '@src/components/BackToTop.astro';

interface Props {
  title: string;
  description?: string;
}

const {
  title = "El Bergantí & Hostalet de Tossa",
  description = "Disfruta de la auténtica Costa Brava. Dos alojamientos con encanto en el centro de Tossa de Mar con restaurante y reserva directa.",
} = Astro.props;
---
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <!-- Primary Meta -->
    <meta name="description" content={description} />
    <meta name="author" content="Author Name" />
    <meta name="robots" content="index, follow" />

    <link rel="canonical" href={Astro.url?.href || "https://nordicia.madethemes.com"} />
    <link rel="icon" type="image/x-icon" href="/img/favicon.png" />
    <Font cssVariable="--font-roboto" preload />
    <Font cssVariable="--font-gilda-display" preload />
  </head>

  <body class="font-sans text-gray-800 pt-20">
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <BackToTop />

    <!-- Alpine.js -->
    <script>
      import Alpine from 'alpinejs';
      declare global {
        interface Window {
          Alpine: typeof Alpine;
        }
      }
      window.Alpine = Alpine;
      Alpine.start();
    </script>

    <!-- GLightbox -->
    <script>
      import GLightbox from 'glightbox';

      const hasLightbox = document.querySelector('.glightbox');
      if (hasLightbox) {
        GLightbox({
          selector: '.glightbox',
          touchNavigation: true,
          loop: true,
        });
      }
    </script>

    <script src="@src/scripts/animation.ts"></script>
  </body>
</html>